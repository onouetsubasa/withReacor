# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'withReactor' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for withReactor
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'ReactorKit'
  pod 'R.swift'

  target 'withReactorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'withReactorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
